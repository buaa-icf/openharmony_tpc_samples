## v1.0.0

XSLT processor supports the following capabilities:
*Support parsing XML strings into operable objects.
*Perform XSLT transformation.
*Build an xpath expression to calculate context and computation.
*Support parsing of xpath expressions.
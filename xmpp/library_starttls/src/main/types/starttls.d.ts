// import { Socket } from "net";
// import { ConnectionOptions, TLSSocket } from "tls";

export function canUpgrade(socket: ESObject): boolean;
export function upgrade(socket: ESObject, options?: ESObject): ESObject;
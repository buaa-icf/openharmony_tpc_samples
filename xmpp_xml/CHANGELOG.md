# Changelog

## [v1.0.0] 2024-05
- 创建xml元素。
- 解析XML字符串。
- 含有特殊字符XML字符串进行转义。
- 将转义后的XML字符串进行反转义 。
- 将XML文本中的特殊字符进行转义。
- 将转义后的XML文本进行反转义


基于@xmpp/xml 原库0.13.0版本进行适配，使其可以运行在 OpenHarmony，并沿用其现有用法和特性。已支持：
- 创建xml元素。
- 解析XML字符串。
- 含有特殊字符XML字符串进行转义。
- 将转义后的XML字符串进行反转义 。
- 将XML文本中的特殊字符进行转义。
- 将转义后的XML文本进行反转义
## [1.0.0] 

### Added
- Initial release of SwipeMenuListView component.
- Support for left and right swipe gestures to reveal action menus.
- Core APIs: `create()`, `addMenuItem()`, `open()`, `close()`, `setOnMenuItemClickListener()` etc.
- Smooth animations and touch state management.
